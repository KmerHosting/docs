---
title: "SDKs"
description: "Use an official server-side SDK for the KmerHosting API."
---

Official SDKs expose the same KmerHosting resource families while giving each language a native package and calling style.

## Choose your SDK

<CardGroup cols={2}>
  <Card title="TypeScript">
    <img src="/images/languages/typescript.svg" alt="TypeScript logo" width="48" height="48" />

    `npm install @kmerhosting/sdk`
  </Card>

  <Card title="Python">
    <img src="/images/languages/python.svg" alt="Python logo" width="48" height="48" />

    `pip install kmerhosting-sdk`
  </Card>

  <Card title="PHP">
    <img src="/images/languages/php.svg" alt="PHP logo" width="64" height="48" />

    `composer require kmerhosting/sdk`
  </Card>

  <Card title="Java 17+">
    <img src="/images/languages/java.svg" alt="Java logo" width="48" height="48" />

    `com.kmerhosting:kmerhosting-sdk`
  </Card>
</CardGroup>

All clients read `KMERHOSTING_API_KEY` by default and expose the same resource families: `account`, `services`, `domains`, `email`, `hosting`, `lxc`, and `kvm`. Write methods generate idempotency keys if none is supplied. The former `vps` resource is retired.

## Examples

<Tabs>
  <Tab title="TypeScript">
    ```ts
    import { KmerHostingClient } from "@kmerhosting/sdk";

    const client = new KmerHostingClient({ apiKey: process.env.KMERHOSTING_API_KEY });
    const services = await client.services.list();
    ```
  </Tab>

  <Tab title="Python">
    ```python
    from kmerhosting import KmerHostingClient

    client = KmerHostingClient()
    services = client.services.list()
    ```
  </Tab>

  <Tab title="PHP">
    ```php
    use KmerHosting\Client;

    $client = new Client();
    $services = $client->services()->all();
    ```
  </Tab>

  <Tab title="Java">
    ```xml
    <dependency>
      <groupId>com.kmerhosting</groupId>
      <artifactId>kmerhosting-sdk</artifactId>
      <version>0.2.0</version>
    </dependency>
    ```

    ```java
    var client = new KmerHostingClient();
    var services = client.services().list();
    ```
  </Tab>
</Tabs>

Java returns Jackson `JsonNode` API envelopes. See each package README and the [API reference](/api-reference) for method-to-endpoint mapping.
